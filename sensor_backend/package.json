{
  "name": "sensor_backend",
  "version": "1.0.0",
  "description": "The locally hosted server which listens for raw data packets from the sensor modules and passes them to the database",
  "main": "sensor_backend.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Doug Rynar",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "https": "^1.0.0",
    "net": "^1.0.2",
    "pg": "^8.11.3"
  }
}
